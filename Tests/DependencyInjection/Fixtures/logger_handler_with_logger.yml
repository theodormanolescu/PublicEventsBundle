imports:
    - { resource: event_dispatcher_and_logger.yml }

elefant_public_events:
    handlers:
        logger_test:
            type: logger
            filters:
                - {name: regex1}
                - {name: regex2}
                - {class: ClassName}
